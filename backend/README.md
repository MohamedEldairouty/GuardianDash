# ☁️ GuardianDash Backend

Node.js + TypeScript API and WebSocket server.
Receives telemetry from the STM32 device, validates crash events, dispatches push notifications, and triggers Twilio emergency calls.

> 🚧 To be built in a later step. The mobile app currently runs against a mock telemetry service.
